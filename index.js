/**
 * Archivo de entrada principal para la librería MiniToast.
 * Exporta la clase MiniToast para facilitar su uso en proyectos.
 *
 * @module MiniToast
 * @example
 * import MiniToast from 'minitoast';
 * const toast = new MiniToast();
 * toast.success({ title: 'Éxito', message: 'Operación completada' });
 */

// Exportar la clase MiniToast desde el archivo fuente
module.exports = require('./src/js/app.js');
